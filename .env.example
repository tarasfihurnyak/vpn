DB_USER=postgres
DB_PASSWORD=postgres
DB_HOST=db
DB_PORT=5432
DB_NAME=vpn
DB_SSLMODE=disable

# JWT_PRIVATE_KEY_FILE=certs/jwt_private.pem  (default; override if needed)
# JWT_ACCESS_TTL=15m
# JWT_REFRESH_TTL=168h
# JWT_SECURE_COOKIE=true
# JWT_ALLOWED_ORIGINS=http://localhost

# Bootstrap: creates the first admin user on startup if no users exist.
# Remove or leave ADMIN_PASSWORD empty after the first run.
# ADMIN_USERNAME=admin
# ADMIN_EMAIL=admin@gmail.com
ADMIN_PASSWORD=ChangeMe123!
